package Abstraction;

public interface Transport {
//    in interface every method is public and abstract. and we don't need to explicitly write it
//    An interface can have static method with implementation as it will be of the class not the object
    public abstract void getSetGo();
}
